<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= doc.title %></title>
</head>
<body>
<%- include('../partials/header.ejs') %>

<h1><%= doc.title %></h1>
<p><%= doc.description %></p>
<p>Autor: <%= doc.author %></p>
<p>Dodano: <%= new Date(doc.uploaded_at).toLocaleDateString() %></p>
<p>Użytkownik: <%= doc.username %></p>
<p><a href="/uploads/<%= doc.filename %>" download="<%= doc.title.replaceAll(' ', '_') + "." + doc.file_ext %>">Pobierz plik</a></p>

<% if (user?.username === doc.username) { %>
    <a href="/edit-document/<%= doc.id %>">Edytuj</a>
    <a href="/delete-document/<%= doc.id %>" id="delete-document-link">Usuń</a>
    <script src="/js/deleteDocumentHandler.js"></script>
<% } %>
</body>

</html>
